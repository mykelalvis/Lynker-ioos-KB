# Model Builder

A Model Builder takes a [[Builder Specification]] and performs processing against that to produce a [[Compiled Executable Model]].

It is sufficient to think of the process as "compiling" the provided specification down to an "executable".

Successful processing of a valid [[Builder Specification]] produces a [[Compiled Executable Data Package]], which is itself conformant to the [[Compiled Executable Data Package Specification]].  

