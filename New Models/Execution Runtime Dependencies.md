# Execution Runtime Dependencies

An execution runtime dependency is an [[Dependency]] that is only required at [[Execution]] time.  

The primary execution dependencies are 
- a built executable model (by way of container, AMI image, or just a bundle of executable files)
- [[inbound data]]

