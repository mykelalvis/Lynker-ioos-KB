First and foremost, all tasks are "handed" a set of named filesystems, which are mounted as a tree of information.  The names are themselves mapped to an ordered tree of mount points on the executable machinery.

Second, the execution is ultimately an execution of an underlying heavy-weight process on the OS.

Finally, the output location is something that the task is provided in the same format as the input data.  

