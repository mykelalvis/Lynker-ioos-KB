# Realized Catalog

 A catalog is considered "realized" when the only required additional inputs to execution are run-time variables.  This implies that all [[Execution Runtime Dependencies]] have been resolved and any relevant values have been injected into the (single) file that c